import Player from '@components/Actors/Player'

/**
 * Class for admin characters for use in-game. Will allow use of some admin functions within the game.
 */

export default class Immortal extends Player {

    constructor( data ) {
        super( data )

    }
}